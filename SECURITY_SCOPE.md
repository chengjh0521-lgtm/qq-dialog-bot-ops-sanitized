# 发布白名单与排除清单

本仓库只允许以下类型的非敏感内容：

- 通用 Python 依赖清单；
- 通用虚拟环境初始化脚本；
- 不含任何值的环境变量模板；
- 脱敏且从零编写的部门协作参考代码：接口、数据模型、持久化和服务编排；
- 不透明信号的校验、传递及审计记录代码（不得包含信号产生规则）。

严禁提交：

- 任意算法、策略参数、指标、信号规则、回测代码或研发文档；
- 行情采集实现、策略服务实现、真实数据字段映射、交易日历或任何可反推信号的方法；
- `.env`、Token、密码、QQ/OpenID、邮箱地址、授权码和其他身份信息；
- 数据库、JSONL/CSV/XLSX 台账、日志、缓存和运行输出。

合并前应执行：

```powershell
git status --short
git grep -n -I -E '(api[_-]?key|secret|token|password|auth[_-]?code)'
```

若结果包含真实值，立即停止提交并移除该文件。
